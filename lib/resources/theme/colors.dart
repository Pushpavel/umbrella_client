import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xff048fff);
  static const primaryVariant = Color(0xffeff7ff);
  static const secondary = Color.fromRGBO(85, 85, 85, 1);
  static const secondaryVariant = Color.fromRGBO(255, 184, 0, 1);
  static const error = Color.fromRGBO(255, 94, 4, 1);

  static const whiteHigh = Colors.white;
  static const whiteMedium = Color(0xbcffffff); // white74
  static const whiteDisabled = Colors.white38;

  static const blackHigh = Colors.black87;
  static const blackMedium = Color(0x99000000); // black60
  static const blackDisabled = Colors.black38;

  static const googleBlue = Color(0xff346ef1);
}
